# Formatting will silently fail if rustfmt fails to reduce a string's width
format_strings = true

# Preference
fn_call_width = 80
fn_params_layout = "Compressed"
imports_granularity = "Item"
wrap_comments = true
